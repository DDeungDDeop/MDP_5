<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.useopencvwithndk_build" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/dogcopy"                       -->어플아이콘을 drawable폴더의 dogcopy.png로
        android:label="@string/app_name"                       -->어플 이름설정
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"      -->상단메뉴바 없앰
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".IntroActivity">          -->실행할 액티비티 이름
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity">                  --->실행할 액티비티 이름
        </activity>
    </application>
</manifest>